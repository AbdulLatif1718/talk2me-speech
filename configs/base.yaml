dataset:
  name: default
  language: en
  sampling_rate: 16000

model:
  name: whisper-small
  cache_dir: ./models

training:
  epochs: 5
  batch_size: 8
  learning_rate: 5e-5
  output_dir: ./models/checkpoints

inference:
  device: cuda
  fp16: true
